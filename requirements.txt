# Core GUI
pywebview>=6.1

# Token storage
keyring>=25.0.0

# LLM providers
anthropic>=0.40.0
openai>=1.0.0

# Google Gemini (image + video generation)
google-genai>=1.9.0

# Audio recording
sounddevice>=0.4.6
numpy>=1.24.0

# Transcription / diarization (heavy — installed separately, not bundled in .app)
# whisperx>=3.7.4
# pyannote.audio>=3.3.0
# faster-whisper>=1.0.0
# torch>=2.0.0

# macOS / pywebview dependencies (installed automatically via pywebview on macOS)
# pyobjc-framework-Cocoa, pyobjc-framework-WebKit
